ReadMe

Updated drink class to incorporate a comparable interface- - Updated Snack class to incorporate a comparable interface-Use of compareTo() method and tested 

User must run the "Driver" class to execute the program.

The driver class creates a Dispenser object, loads the dispenser and launches the displayProducts() method.

The programs stores 60 products in the "productArray". The productArray is displayed to the screen. The display shows the product number, product Type, product company, product name, product weight, product price, and product cost.

To test the compareTo() method (2) drink objects were created. The drink objects are equal therefore the display is expected to be zero. (2) candy objects were created. The candy objects are equal. Candy1 one is less than Candy2 therefore the display is expected to be a negative number.